***Тестовый проект (Django + React)***

## Как запустить

### `sudo cp .example.env .env`
### `sudo docker-compose -f docker-compose.yml up`


Swagger `http://localhost:8000/swagger/`

Frontend `http://localhost:3000/load`